# Disables completely the safe-path feature
set auto-load safe-path /

# Prints on gdb.txt file all that happens on gdb console
set logging enabled on

set print pretty on

# It does not breaks into pages when a command has a lot of output
set pagination off

# GDB does not ask for confirmation of commands
set confirm off
